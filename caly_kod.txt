    public partial class Form1 : Form
    {
        public Form1()
        {
            InitializeComponent();
        }

        private void panel1_Paint(object sender, PaintEventArgs e)
        {
            Graphics g = e.Graphics;
            int szer = panel1.ClientRectangle.Width;
            int wys = panel1.ClientRectangle.Height;
            int r = 50;
            Pen pen = new Pen(Color.Green);
            
        }

        private void panel2_Paint(object sender, PaintEventArgs e)
        {
            Graphics g = e.Graphics;
            int szer = panel2.ClientRectangle.Width;
            int wys = panel2.ClientRectangle.Height;
            int r = 50;
            g.DrawRectangle(Pens.Green, szer / 4-8, wys / 4+70,  r*2,  r*2);
            g.FillRectangle(Brushes.Blue, szer / 2 - 30, wys / 2, r, r);
            Font myFont = new System.Drawing.Font("Helvetica", 17, FontStyle.Italic);

        }

        private void panel3_Paint(object sender, PaintEventArgs e)
        {
            Graphics g = e.Graphics;
            int szer = panel3.ClientRectangle.Width;
            int wys = panel3.ClientRectangle.Height;
            int r = 50;
            Font myFont = new System.Drawing.Font("Helvetica", 17, FontStyle.Italic);
            Pen pen = new Pen(Color.Green);
            g.DrawString("Wielka", myFont, Brushes.Black, szer / 2 , wys / 2 -r/2);
            g.DrawString("Noc", myFont, Brushes.Black, szer / 2 , wys / 2  );
        }

        private void panel4_Paint(object sender, PaintEventArgs e)
        {
            Graphics g = e.Graphics;
            int szer = panel4.ClientRectangle.Width;
            int wys = panel4.ClientRectangle.Height;
            g.DrawLine(Pens.Aqua, 0, 0, szer, wys);
        }

        private void panel5_Paint(object sender, PaintEventArgs e)
        {
            Graphics g = e.Graphics;
            int szer = panel5.ClientRectangle.Width;
            int wys = panel5.ClientRectangle.Height;
            int r = 50;
            Pen pen = new Pen(Color.Green);
            g.DrawEllipse(Pens.White, szer / 2, wys / 2, r,  r);
            g.DrawArc(Pens.Black, 100, 100, 50, 50, 0, 180);
        }
    }